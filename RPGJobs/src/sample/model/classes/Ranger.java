package sample.model.classes;

import sample.model.personagem.Personagem;

public class Ranger extends Personagem {
    public Ranger(String usuario, String senha, String email, String nick) {
        super(usuario, senha, email, nick);
    }
}
